# RAGuard AI

> An AI system that knows when it does not know.

Advanced self-correcting RAG system with multi-agent verification,
contradiction detection, and confidence-aware answering.

## Status
🚧 Milestone 1 — Project scaffolding complete.

## Run Backend
cd backend && source .venv/bin/activate && uvicorn main:app --reload

## Run Frontend
cd frontend && npm run dev